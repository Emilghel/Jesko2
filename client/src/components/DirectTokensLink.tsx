import React from 'react';

// This component was previously providing direct links to the token checkout page
// Now disabled as requested by the user

const DirectTokensLink = () => {
  // Return null to make the component disappear from the UI
  return null;
};

export default DirectTokensLink;