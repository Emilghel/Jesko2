// This script is designed to handle redirects for Character Selection pages
window.onload = function() {
  // Wait a moment to ensure everything is loaded
  setTimeout(function() {
    // Navigate directly to the 3D character selection page
    window.location.href = '/character-selection-3d';
  }, 100);
};